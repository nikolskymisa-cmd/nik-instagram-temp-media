services:
  - type: web
    name: nik-posts-telegram-receiver
    env: python
    plan: free
    buildCommand: pip install -r render_requirements.txt
    startCommand: gunicorn render_telegram_receiver:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
